const concatStrings = (a: string, b: string) => {
    console.log(`${a} ${b}`);
}

const addNumbers = (a: number, b: number) => {
    console.log(`Sum is ${a + b}`);
}

concatStrings("Akshay", "Khurana");
addNumbers(1, 2);